#!/usr/bin/env bash
# exit on error
set -o errexit

echo "📦 Installing dependencies..."
bundle install

echo "✅ Build complete!"
echo "💡 Run 'rails db:migrate' in Render Shell to set up the database"
