#!/usr/bin/env bash
# bin/test-db

case "$1" in
  setup|reset)
    echo "🧪 Setting up test database..."
    rm -f db/test.sqlite3*
    env -u DATABASE_URL RAILS_ENV=test rails db:migrate
    echo "✅ Done"
    ;;
  *)
    echo "Usage: ./bin/test-db setup"
    exit 1
    ;;
esac